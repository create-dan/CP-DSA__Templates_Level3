Time Complexity of Harmonic Series : - n(logn)


int lcm(int a, int b)
{
    return a / __gcd(a, b) * b;
}



- Set uses Binary Search Trees for implementation
- Unordered Set uses Hash Tables for implementation



- Typecasting

char ch = s[i];
let's say  ch = '2'
freq = ch - '0';
freq= 2 (in int)


 // Creates a min heap
    priority_queue <int, vector<int>, greater<int> > pq;
    

 transform(ans.begin(), ans.end(), ans.begin(), ::tolower);
 
 
**next_permutation
 
int arr[] = { 1, 2, 3 };
sort(arr, arr + 3);

 do {
        cout << arr[0] << " " << arr[1] << " " << arr[2] << "\n";
  } while (next_permutation(arr, arr + 3));
  
  
  
  
  https://www.geeksforgeeks.org/priority-queue-set-1-introduction/
  
   fill(nums.begin(),nums.end(),0);
   
   
   
*Agar graph ke andr cycle present hai to ham topological sort nahi laga sakte
  

  
  
  





  
  
